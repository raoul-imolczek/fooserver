{
    "package": {
        "name": "fooserver",
        "repo": "raoul-imolczek-maven"
    },

    "version": {
        "name": "@VERSION@",
        "subject": "SNAPSHOT upload",
        "gpgSign": false
    },

  "files":
  [
    {"includePattern": "build/libs/(.*\.jar)", "uploadPattern": "@VERSION@/$1"},
    {"includePattern": "build/(.*\.pom)",  "uploadPattern": "@VERSION@/$1"}
  ],

  "publish": true
}
